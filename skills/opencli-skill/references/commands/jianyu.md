# jianyu

## Commands

### search
- Purpose: 搜索剑鱼标讯公告
- Args:
  - `query`(required); Search keyword, e.g. "procurement"
  - `limit`(optional; type: int; default: 20); Number of results (max 50)
- Usage: `opencli jianyu search [options] -f json`
