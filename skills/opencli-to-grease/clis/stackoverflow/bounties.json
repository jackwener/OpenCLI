{
  "actions": [
    {
      "action": "open",
      "argument": {
        "url": "https://stackoverflow.com",
        "waitUntil": "load"
      }
    },
    {
      "action": "evaluate",
      "argument": {
        "script": "async (intermediate) => { const res = await fetch('https://api.stackexchange.com/2.3/questions/featured?order=desc&sort=activity&site=stackoverflow'); const data = await res.json(); return (data?.items || []).slice(0, {{ limit }}).map((item, i) => ({ rank: i + 1, bounty: item.bounty_amount, title: item.title, score: item.score, answers: item.answer_count, url: item.link })); }"
      }
    }
  ],
  "api_endpoint": "stackoverflow-bounties",
  "category": "content",
  "description": "Active bounties on Stack Overflow",
  "is_public": true,
  "method": "GET",
  "name": "Bounties",
  "output_schema": [
    {
      "name": "rank",
      "type": "number",
      "description": "排名"
    },
    {
      "name": "bounty",
      "type": "number",
      "description": "赏金"
    },
    {
      "name": "title",
      "type": "string",
      "description": "标题"
    },
    {
      "name": "score",
      "type": "number",
      "description": "得分"
    },
    {
      "name": "answers",
      "type": "number",
      "description": "回答数"
    },
    {
      "name": "url",
      "type": "string",
      "description": "链接"
    }
  ],
  "variables": [
    {
      "name": "limit",
      "type": "int",
      "default": 10,
      "help": "Max number of results"
    }
  ],
  "website_domain": "stackoverflow.com",
  "website_id": "website-stackoverflow.com"
}