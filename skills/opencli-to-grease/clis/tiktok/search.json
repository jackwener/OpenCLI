{
  "actions": [
    {
      "action": "open",
      "argument": {
        "url": "https://www.tiktok.com/explore",
        "waitUntil": "load"
      }
    },
    {
      "action": "wait",
      "argument": {
        "timeMs": 5000
      }
    },
    {
      "action": "evaluate",
      "argument": {
        "script": "async (intermediate) => { const query = \"{{ query }}\"; const limit = {{ limit }} || 10; const res = await fetch('/api/search/general/full/?keyword=' + encodeURIComponent(query) + '&offset=0&count=' + limit + '&aid=1988', { credentials: 'include' }); if (!res.ok) throw new Error('Search failed: HTTP ' + res.status); const data = await res.json(); const items = (data.data || []).filter(function(i) { return i.type === 1 && i.item; }); return items.slice(0, limit).map(function(i, idx) { var v = i.item; var a = v.author || {}; var s = v.stats || {}; return { rank: idx + 1, desc: (v.desc || '').replace(/\\n/g, ' ').substring(0, 100), author: a.uniqueId || '', url: (a.uniqueId && v.id) ? 'https://www.tiktok.com/@' + a.uniqueId + '/video/' + v.id : '', plays: s.playCount || 0, likes: s.diggCount || 0, comments: s.commentCount || 0, shares: s.shareCount || 0 }; }); }"
      }
    }
  ],
  "api_endpoint": "tiktok-search",
  "category": "scrape",
  "description": "Search TikTok videos",
  "is_public": true,
  "method": "GET",
  "name": "Search",
  "output_schema": [
    {
      "name": "rank",
      "type": "number",
      "description": "排名"
    },
    {
      "name": "desc",
      "type": "string",
      "description": "描述"
    },
    {
      "name": "author",
      "type": "string",
      "description": "作者"
    },
    {
      "name": "url",
      "type": "string",
      "description": "视频链接"
    },
    {
      "name": "plays",
      "type": "number",
      "description": "播放量"
    },
    {
      "name": "likes",
      "type": "number",
      "description": "点赞数"
    },
    {
      "name": "comments",
      "type": "number",
      "description": "评论数"
    },
    {
      "name": "shares",
      "type": "number",
      "description": "分享数"
    }
  ],
  "variables": [
    {
      "name": "query",
      "type": "string",
      "required": true,
      "help": "Search query"
    },
    {
      "name": "limit",
      "type": "int",
      "default": 10,
      "help": "Number of results"
    }
  ],
  "website_domain": "www.tiktok.com",
  "website_id": "website-tiktok"
}
