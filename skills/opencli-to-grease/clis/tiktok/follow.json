{
  "actions": [
    {
      "action": "open",
      "argument": {
        "url": "https://www.tiktok.com/@{{ username }}",
        "waitUntil": "load"
      }
    },
    {
      "action": "wait",
      "argument": {
        "time": 6000
      }
    },
    {
      "action": "evaluate",
      "argument": {
        "script": "async (intermediate) => { const username = \"{{ username }}\"; const buttons = Array.from(document.querySelectorAll('button, [role=\"button\"]')); const followBtn = buttons.find(function(b) { var text = b.textContent.trim(); return text === 'Follow' || text === '关注'; }); if (!followBtn) { var isFollowing = buttons.some(function(b) { var t = b.textContent.trim(); return t === 'Following' || t === '已关注' || t === 'Friends' || t === '互关'; }); if (isFollowing) return [{ status: 'Already following', username: username }]; return [{ status: 'Follow button not found', username: username }]; } followBtn.click(); await new Promise(r => setTimeout(r, 2000)); return [{ status: 'Followed', username: username }]; }"
      }
    }
  ],
  "api_endpoint": "tiktok-follow",
  "category": "interact",
  "description": "Follow a TikTok user",
  "is_public": false,
  "method": "POST",
  "name": "Follow",
  "output_schema": [
    {
      "name": "status",
      "type": "string",
      "description": "状态"
    },
    {
      "name": "username",
      "type": "string",
      "description": "用户名"
    }
  ],
  "variables": [
    {
      "name": "username",
      "type": "string",
      "required": true,
      "help": "TikTok username (without @)",
      "test": "funnydogsa"
    }
  ],
  "website_domain": "www.tiktok.com",
  "website_id": "website-tiktok"
}
