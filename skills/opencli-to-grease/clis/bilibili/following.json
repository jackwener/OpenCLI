{
  "actions": [
    {
      "action": "open",
      "argument": {
        "url": "https://www.bilibili.com",
        "waitUntil": "networkidle"
      }
    },
    {
      "action": "evaluate",
      "argument": {
        "script": "async (intermediate) => { const uid = {{ uid }}; const page = {{ page }} || 1; const limit = Math.min({{ limit }} || 50, 50); const resp = await fetch(`https://api.bilibili.com/x/relation/followings?vmid=${uid}&pn=${page}&ps=${limit}&order=desc`, { credentials: 'include' }); const payload = await resp.json(); if (payload.code !== 0) { return [{ mid: '-', name: `获取关注列表失败: ${payload.message} (${payload.code})`, sign: '', following: '', fans: '' }]; } const list = payload.data?.list || []; if (list.length === 0) { return [{ mid: '-', name: `共 ${payload.data?.total ?? 0} 人关注，当前页无数据`, sign: '', following: '', fans: '' }]; } return list.map(u => ({ mid: u.mid, name: u.uname, sign: (u.sign || '').slice(0, 40), following: u.attribute === 6 ? '互相关注' : '已关注', fans: u.official_verify?.desc || '' })); }"
      }
    }
  ],
  "api_endpoint": "bilibili-following",
  "category": "auth",
  "description": "获取 Bilibili 用户的关注列表",
  "is_public": true,
  "method": "GET",
  "name": "Following",
  "output_schema": [
    {
      "name": "mid",
      "type": "number",
      "description": "用户ID"
    },
    {
      "name": "name",
      "type": "string",
      "description": "用户名"
    },
    {
      "name": "sign",
      "type": "string",
      "description": "签名"
    },
    {
      "name": "following",
      "type": "string",
      "description": "关注状态"
    },
    {
      "name": "fans",
      "type": "string",
      "description": "认证信息"
    }
  ],
  "variables": [
    {
      "name": "uid",
      "type": "number",
      "required": true,
      "help": "目标用户 ID"
    },
    {
      "name": "page",
      "type": "int",
      "default": 1,
      "help": "页码"
    },
    {
      "name": "limit",
      "type": "int",
      "default": 50,
      "help": "每页数量 (最大 50)"
    }
  ],
  "website_domain": "www.bilibili.com",
  "website_id": "website-bilibili"
}
