{
  "actions": [
    {
      "action": "open",
      "argument": {
        "url": "https://search.smzdm.com/?c=home&s={{ query }}&v=b",
        "waitUntil": "domcontentloaded"
      }
    },
    {
      "action": "wait",
      "argument": {
        "timeMs": 3000
      }
    },
    {
      "action": "scroll",
      "argument": {
        "target": "页面滚动加载更多内容"
      }
    },
    {
      "action": "wait",
      "argument": {
        "timeMs": 1500
      }
    },
    {
      "action": "evaluate",
      "argument": {
        "script": "async (intermediate) => { const limit = {{ limit }} || 20; const items = document.querySelectorAll('li.feed-row-wide'); const results = []; items.forEach((li) => { if (results.length >= limit) return; const titleEl = li.querySelector('h5.feed-block-title > a') || li.querySelector('h5 > a'); if (!titleEl) return; const title = (titleEl.getAttribute('title') || titleEl.textContent || '').trim(); const url = titleEl.getAttribute('href') || titleEl.href || ''; const priceEl = li.querySelector('.z-highlight'); const price = priceEl ? priceEl.textContent.trim() : ''; let mall = ''; const mallEl = li.querySelector('.z-feed-foot-r .feed-block-extras span') || li.querySelector('.z-feed-foot-r span'); if (mallEl) mall = mallEl.textContent.trim(); const commentEl = li.querySelector('.feed-btn-comment'); const comments = commentEl ? parseInt(commentEl.textContent.trim()) || 0 : 0; results.push({ rank: results.length + 1, title, price, mall, comments, url }); }); return results; }"
      }
    }
  ],
  "api_endpoint": "smzdm-search",
  "category": "search",
  "description": "什么值得买搜索好价",
  "is_public": true,
  "method": "GET",
  "name": "Search",
  "output_schema": [
    { "name": "rank", "type": "number", "description": "排名" },
    { "name": "title", "type": "string", "description": "商品标题" },
    { "name": "price", "type": "string", "description": "价格" },
    { "name": "mall", "type": "string", "description": "商家" },
    { "name": "comments", "type": "number", "description": "评论数" },
    { "name": "url", "type": "string", "description": "链接" }
  ],
  "variables": [
    { "name": "query", "type": "string", "required": true, "help": "搜索关键词", "test": "iPhone" },
    { "name": "limit", "type": "int", "default": 20, "help": "返回数量", "test": 30 }
  ],
  "website_domain": "www.smzdm.com",
  "website_id": "website-www.smzdm.com"
}